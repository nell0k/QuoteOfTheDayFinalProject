# QuoteANDEventOfTheDayFinalProject

#when user runs the program first it is displaying the random quote with Author from the API
#then user is requested to type-in correct way the current day and month
#based on the user input program is displaying the event which was happening on the same day and month on the random year
#if the user input has a typo or not matching the description, user requested to repeat the input in the correct form
#we provided exceptions to avoid of crashing of the program
#junit test of the user input required